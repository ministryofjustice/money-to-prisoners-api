# copy this sample file to `local.py` and edit for local settings that will NOT be committed
# these are overlayed on top of base settings

# DATABASES = {
#     'default': {
#         'ENGINE': 'django.db.backends.postgresql',
#         'NAME': 'mtp_api',
#         'USER': 'postgres',
#         'PASSWORD': '',
#         'HOST': '127.0.0.1',
#         'PORT': '5432',
#     },
# }

EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
